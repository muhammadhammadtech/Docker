version: '3.8'

services:
  web:
    image: nginx:alpine
    ports:
      - "8081:80"
    deploy:
      replicas: 2
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  default:
    driver: overlay
