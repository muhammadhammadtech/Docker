version: '3.8'

services:
  web:
    image: nginx:latest
    ports:
      - "8080:80"
    deploy:
      replicas: 3
    networks:
      - webnet

  redis:
    image: redis:alpine
    deploy:
      replicas: 2
    networks:
      - webnet

networks:
  webnet:
    driver: overlay
